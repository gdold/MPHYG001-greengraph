installation: sudo python setup.py install

usage: greengraph.py [-h] [--from START] [--to END] [--steps STEPS]
                     [--out OUT] [--format FORMAT]

Generates a graph of the proportion of green pixels in a series of satellite
images between two points

optional arguments:
  -h, --help       show this help message and exit
  --from START     Starting location, defaults to New York
  --to END         End location, defaults to Chicago
  --steps STEPS    Number of steps between start and end locations, default 20
  --out OUT        Filename to save plot to. Displays plot on screen instead
                   if not specified
  --format FORMAT  Optionally force an output format, overriding extension in
                   --out